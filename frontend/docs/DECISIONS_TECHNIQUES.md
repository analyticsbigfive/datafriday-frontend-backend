# Décisions techniques — hors périmètre Bertrand

> Bertrand est Product Owner : il tranche les règles métier. Les choix d'architecture, de schéma,
> de performance, de refactor ou de planning technique reviennent au lead (Ulrich) — ils n'ont rien
> à faire dans [`QUESTIONS_A_BERTRAND.md`](QUESTIONS_A_BERTRAND.md), qui doit rester réservé aux
> vraies questions produit.
>
> Ce fichier trace ces décisions techniques : chacune ci-dessous est passée par la même revue de
> code que le tracker Bertrand (session du 2026-07-24, chaque question rejouée contre le code/
> schéma/données réels avant classification) et porte une **recommandation évidence-based**. Statut
> `⏳ À confirmer` = recommandation prête, en attente du feu vert d'Ulrich avant implémentation ;
> `✅ Décidé` = confirmé, prêt à coder ; `🔧 Codé` = fait.

| # | Sujet | Recommandation (analyse 2026-07-24) | Statut | Repliée dans |
|---|---|---|---|---|
| 10 | Restock — rôles « Technicien Logistic »/« PDV Superviseur » ont `restockBoard` sans `restock` → `PUT/DELETE` en 403 permanent, travail non sauvegardé. | 0 utilisateur assigné à ces rôles actuellement (audit DB 2026-07-24) : plus un choix de politique produit, juste une incohérence front/back à corriger sans risque de régression. **Gater le front en lecture seule pour ces rôles maintenant** (aligne l'UI sur la vérité serveur, tue le bug de perte silencieuse) ; élargir `PUT/DELETE` à `restockBoard` reste une feature à part si un besoin réel apparaît. | ⏳ À confirmer | [`bugs/19_restock_403_silencieux_front.md`](bugs/19_restock_403_silencieux_front.md), [`backend/docs/bugs/31_restock_403_silencieux_roles_board.md`](../../backend/docs/bugs/31_restock_403_silencieux_roles_board.md) |
| 11 | Inventaire — double persistance `InventoryCount` (granulaire) vs `InventorySnapshot` (blob), quel chemin est canonique ? | Usage réel mesuré (audit DB) : `InventoryCount` a 134 lignes/4 events, dernière écriture 2026-07-23 ; `InventorySnapshot` n'a plus reçu une seule écriture non-legacy depuis le 2026-07-18 — c'est du poids mort, pas une feature d'historique maintenue. **Formaliser `InventoryCount` comme canonique**, enrichir son DTO des champs que seul le snapshot porte aujourd'hui (`discardedQuantity`, `storageLocation`), arrêter d'écrire de nouveaux snapshots (garder les existants en lecture seule pour l'affichage legacy). | ⏳ À confirmer | [`bugs/183_double_persistance_comptages.md`](bugs/183_double_persistance_comptages.md) |
| 12 | Predict — poser la contrainte unique DB nécessite-t-il un nettoyage préalable des doublons (jusqu'à 76 évoqués) ? | Audit DB 2026-07-24 : 0 doublon réel, 3 versions au total. La fiche 181 ne cite aucune source de mesure vérifiable pour le chiffre de 76 (contrairement à la fiche 21, qui documente ses requêtes) — probablement mesuré sur un autre environnement ou le localStorage front, pas cette base. **Poser la contrainte unique maintenant, sans nettoyage** ; noter dans la fiche 181 que le chiffre de 76 est invérifiable en l'état, sans bloquer dessus. Autorisation de migration déjà couverte par ADR-0002. | ⏳ À confirmer | [`bugs/181_cascade_duplication_versions_predict.md`](bugs/181_cascade_duplication_versions_predict.md) |
| 15 | Mapping PdV — `WeezeventLocationShopMapping.weezeventLocationId` : convention double (location vs merchant) sans discriminant, le commentaire du schéma dit « merchant ». | Audit DB 2026-07-24 : 418/418 mappings réels sont keyés **location**, 0 keyé merchant — la convention merchant n'existe nulle part dans les données. `space-aggregation.service.ts:184-185` joint pourtant sur `t."merchantId"`, une jointure donc **fonctionnellement morte** (toujours 0 résultat). **Corriger la jointure sur `t."locationId"` (ou supprimer le code mort), corriger le commentaire du schéma.** Aucune migration de données nécessaire — le nettoyage est purement du code. | ⏳ À confirmer | [`backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md) |
| 16 | Analyse — attribution event↔ventes : fenêtre de dates (item-level) vs lien Weezevent stocké (RPC shop-details), lequel est canonique ? | Ce choix a déjà été tranché une fois : backend bug 21 (🟢 corrigé 2026-07-21) a remplacé la jointure par égalité de date par le lien stocké pour la RPC shop-details, précisément parce que des collisions same-day réelles avaient été trouvées. L'item-level n'a simplement jamais été migré pour suivre — et le lien n'est rempli que sur 8/61 events (13 %) à ce jour. **Terminer d'appliquer la décision déjà prise** : élargir le backfill de `weezeventEventId` (étendre le script one-shot de la fiche 21), puis faire jointer `getEventTimelineBatch` sur le lien stocké comme la RPC, avec repli sur la fenêtre de dates seulement si le lien est encore vide. | ⏳ À confirmer | [`backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md), [`backend/docs/bugs/21_jointure_event_weezevent_par_date_seule.md`](../../backend/docs/bugs/21_jointure_event_weezevent_par_date_seule.md) |
| 21 | Post-event Inventory — Qty Pred lue via pont localStorage (pas cross-device) ; basculer sur l'API `EventPredictVersion.predictedRecords` ? | Audit DB 2026-07-24 : les 3 `EventPredictVersion` en base ont toutes `predictedRecords` non vide — aucun blocage de données. La valeur affichée ne change pas, seule sa disponibilité change (cross-device). **Basculer la lecture sur l'API**, garder le pont localStorage comme fast-path/repli optionnel. | ⏳ À confirmer | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §7.5 |
| 24-D2 | Pre-event — fenêtre `StockMovement` sans borne haute, ancrage sur le dernier match passé **global** plutôt que le match précédant la cible. | Volet distinct de la question métier #25 (raisons de mouvement comptées) : la réponse de Bertrand à #24 (2026-07-24 — reset auto sur Door opening, ancrage sur l'event) règle ce point une fois implémentée — l'ancrage devient l'event lui-même, plus une fenêtre ouverte. **Suivi technique : implémenter le déclencheur du reset "Door opening"** (non fait à ce jour) ; une fois en place, D2 disparaît de lui-même. | ⏳ À confirmer (bloqué sur l'implémentation de #24) | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §8.5, §9.3 (D2) |
| 28-linéaire | RH — source du « mètre linéaire » par shop pour les seuils de staffing. | Sous-partie séparable de la question métier #28 (formules) : `SpaceElement.width` est déjà identifié dans la doc comme candidat (`modules/11_RH_STAFFING.md` §6.2, "Prérequis données") — vérifier que ce champ est bien peuplé pour tous les éléments de type shop et représente effectivement un mètre linéaire de façade (pas une autre dimension). Vérification de mapping de données, pas une question métier. | ⏳ À confirmer | [`modules/11_RH_STAFFING.md`](modules/11_RH_STAFFING.md) §6.2 |
| 29 | RH — persister `HrSupplier`/`StaffPosition` en base plutôt que localStorage (étape 2). | Le principe (donnée métier partagée multi-device/multi-utilisateur → vraie base, pas localStorage) n'est pas discutable techniquement : c'est déjà noté comme limite assumée de l'étape 1 dans `modules/11_RH_STAFFING.md` §3. Seule une question de **planning** peut rester ouverte (caler l'étape 2 avant ou après la validation de l'algo #28) — pas une question de choix technique. | ⏳ À confirmer | [`modules/11_RH_STAFFING.md`](modules/11_RH_STAFFING.md) §3, §6.3 |
| 32 | Post-event Inventory — l'écran de comptage direct (`resolveEventContext`) s'ancre sur le prochain match futur au lieu du dernier passé, contrairement à la réconciliation. | Le code établit déjà la convention exacte demandée par le brief (« dernier événement qui a eu lieu ») pour ce même écran — `resolveReconciliationEvent` l'implémente et `resolveEventContext` a déjà le mode symétrique pour le pre-event (futur strict). Il ne manque que le cas symétrique en mode post. **Recommandation : dans `resolveEventContext`, quand `!isPreMode` et pas de `?event=`, ancrer sur le dernier passé (repli futur seulement si aucun passé n'existe)** — aligne le comportement sur `resolveReconciliationEvent`, sans ambiguïté produit restante (les 2 routes distinctes encodent déjà l'intention utilisateur). | ⏳ À confirmer | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §11.3 |
| Live | Bouton ◉ Home (`SpaceItem.vue`) — polling `live-status` par carte (option a) vs visible seulement après entrée dans l'espace (option b) ? | Le module Live n'existe pas encore sur `develop` (aucune trace dans `frontend/src`) ; il est en cours sur la branche `feat/analyse-liveFeature`, où le choix (a)/(b) est déjà posé sans être tranché. C'est un arbitrage coût réseau/latence, pas une nouvelle règle métier (l'intention produit — afficher le statut live — est déjà actée). **Recommandation : éviter le dilemme en un seul appel batché** (`GET /spaces/live-status?ids=...`) au montage de la Home plutôt que N pollings individuels ou une dégradation de fonctionnalité. | ⏳ À confirmer | *(pas de fiche — module en construction sur `feat/analyse-liveFeature`, `modules/11_LIVE.md` référencé partout mais jamais rédigé sur `develop`)* |
| — | `EventPredictVersion.tenantId` nullable : lignes legacy NULL invisibles de toutes les requêtes scopées tenant. | Audit DB 2026-07-24 : **1 seule** ligne orpheline dans toute la base (event inexistant, donc aucun tenant vers lequel backfiller). Volume trop faible pour justifier un arbitrage produit — **ajouter la vérification d'existence d'`eventId` à la création (déjà fait, cf. Fermées #5), supprimer/nulliser cette unique ligne orpheline, puis passer la colonne NOT NULL.** | ⏳ À confirmer | [`backend/docs/bugs/98_eventpredictversion_tenantid_nullable.md`](../../backend/docs/bugs/98_eventpredictversion_tenantid_nullable.md) |
| — | `StockLevel.elementId` sans FK vers `SpaceElement` — niveaux orphelins (delete+recreate config v1) compensés en lecture. | Les orphelins sont un artefact technique (reconstruction du builder v1), pas une donnée métier à préserver. **Purger les orphelins, poser la FK avec `ON DELETE CASCADE`** (un niveau de stock n'a pas de sens sans son élément) — choix d'intégrité de schéma standard, aucun signal produit nécessaire. | ⏳ À confirmer | [`backend/docs/bugs/101_stocklevel_elementid_sans_fk.md`](../../backend/docs/bugs/101_stocklevel_elementid_sans_fk.md) |
| — | Deux scripts SQL rejouables jamais exécutés : `2026-07-18_inventorycount_unique_nulls_not_distinct.sql` (dédup destructif-par-conception + contrainte unique `NULLS NOT DISTINCT`) et `2026-07-18_perf_indexes_predict_inventory.sql` (index additifs `CONCURRENTLY`). | Confirmé : aucune trace dans `backend/prisma/migrations/` — jamais appliqués. Pure question de **planning de déploiement** (fenêtre de maintenance pour le premier, verrou ACCESS EXCLUSIVE ; le second est sans risque, `CONCURRENTLY`). Ulrich planifie la fenêtre, ce n'est pas un arbitrage produit. | ⏳ À confirmer | [`bugs/93_inventorycount_toctou_unique_nulls.md`](bugs/93_inventorycount_toctou_unique_nulls.md), [`bugs/99_index_perf_manquants_predict_inventory.md`](bugs/99_index_perf_manquants_predict_inventory.md) |

## Historique

- **2026-07-24** : fichier créé en migrant 13 items qui vivaient à tort dans
  [`QUESTIONS_A_BERTRAND.md`](QUESTIONS_A_BERTRAND.md) (dont 4 jamais fusionnés depuis
  `backend/docs/old/QUESTIONS_A_BERTRAND.md`) — chacun rejoué contre le code/schéma/données réels
  avant classification, voir le résumé dans la conversation du 2026-07-24.
