# Questions à Bertrand — tracker de clarification

> On n'a pas de base de code auto-porteuse : la compréhension fonctionnelle réelle passe par
> Bertrand (review, test, validation). Ce fichier évite que chacun tranche seul dans son coin une
> incompréhension du code ou d'une fonctionnalité — ce qui est la source la plus fréquente de bugs
> et de conflits sur ce projet (voir [`docs/bugs/00_INDEX.md`](bugs/00_INDEX.md), plusieurs bugs
> viennent de deux devs ayant chacun implémenté sa propre interprétation de la même règle — ex.
> le gating Team, [BUG côté front](bugs/) vs comportement attendu).

## Comment l'utiliser

1. Bloqué sur une incompréhension (code, règle métier, comportement attendu) → ajouter une ligne
   ci-dessous, statut 🔴.
2. Poser la question à Bertrand.
3. Une fois répondu : mettre à jour la doc canonique concernée (page de module dans
   [`docs/modules/`](modules/00_INDEX.md), [`docs/adr/`](adr/00_INDEX.md), ou
   [`docs/bugs/`](bugs/00_INDEX.md) si la réponse révèle un bug) — **et le code si la réponse
   implique un renommage/commentaire/refactor** — puis lier la mise à jour ici et passer le
   statut à 🟢.
4. **Ne jamais merger un code qui repose sur une hypothèse encore marquée 🔴.**

Même mécanique pour une fonctionnalité neuve : confronter le cahier des charges au code existant,
lister ici ce qui reste ambigu, trancher avec Bertrand avant d'écrire le code définitif.

## Questions ouvertes

| # | Question | Domaine | Posée par | Date | Statut | Repliée dans |
|---|---|---|---|---|---|---|
| 2 | Import CSV `/events` : les colonnes `performerName`/`sponsor`/`openingActName`/`allSessions` sont proposées au mapping mais aucun champ `Event` ne les stocke (root cause probable : copiées depuis `EventsImportWizard.vue`, code mort du sous-arbre `appCopy.vue`, jamais adaptées au vrai `CreateEventDto`). Ajouter ces colonnes au schéma `Event`, ou les retirer du mapping CSV ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/136_csvimportdrawer_champs_hors_dto_400_garanti.md`](bugs/136_csvimportdrawer_champs_hors_dto_400_garanti.md) |
| 3 | Suppression `EventType`/`EventCategory` : cascade silencieuse (via `onDelete: Cascade`) vers catégories/sous-catégories/events-teams orphelins, sans garde "N éléments en dépendent, confirmer ?". Le même pattern existe déjà sans garde sur `ProductType`/`ComponentType` (vérifié dans le code) — ajouter un garde-fou isolément sur Events créerait une incohérence UX avec ces autres taxonomies. Garde-fou transverse à toutes les taxonomies, ou statu quo assumé ? | Événements (transverse) | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`../../api-datafriday-staging/docs/bugs/75_eventtype_eventcategory_delete_cascade_sans_garde.md`](../../api-datafriday-staging/docs/bugs/75_eventtype_eventcategory_delete_cascade_sans_garde.md) |
| 4 | `Team` n'a aucune contrainte `@@unique` en base (contrairement à `EventType`/`EventCategory`/`EventSubcategory`) — le check anti-doublon applicatif est insensible à la casse (`mode: 'insensitive'`), qu'une contrainte DB standard ne reproduit pas nativement. Ajouter la contrainte (et avec quelle sémantique de casse) ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`../../api-datafriday-staging/docs/bugs/70_team_duplicate_toctou_sans_unique_index.md`](../../api-datafriday-staging/docs/bugs/70_team_duplicate_toctou_sans_unique_index.md) |
| 5 | `EventPredictVersion.create()` ne vérifie pas que `eventId` (pris depuis l'URL) existe/appartient au tenant — orphelines possibles sur id erroné/typo. Ajouter la vérification, seule ou avec le nettoyage plus large déjà noté (versions orphelines après suppression d'un Event) ? | Prévision | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`../../api-datafriday-staging/docs/bugs/76_predictversion_create_eventid_non_verifie.md`](../../api-datafriday-staging/docs/bugs/76_predictversion_create_eventid_non_verifie.md) |
| 6 | `/event-categories` (drawer inline, avec édition) et `EventCategoryDialog.vue` (dialog partagé "quick-add", création seule, utilisé pendant un autre flux) sont deux implémentations distinctes de la création de catégorie — cause racine des BUG-130/131 (`hasHomeTeam` géré différemment aux deux endroits). Garder les deux avec un garde-fou (payload partagé/factorisé) pour éviter une nouvelle divergence de champ, ou unifier sur un seul composant (ajouter un mode édition au dialog partagé) ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/145_eventcategorielist_duplication_creation_categorie.md`](bugs/145_eventcategorielist_duplication_creation_categorie.md) |
| 7 | `EventFormDrawer.vue` : aucune validation croisée `ticketsScanned` ≤ `ticketsSold` — règle métier à confirmer (des cas légitimes existent en billetterie, ex. invités hors vente comptés au scan). Si une règle est voulue : bloquante ou avertissement seulement ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/146_eventformdrawer_ticketsscanned_sans_validation_croisee.md`](bugs/146_eventformdrawer_ticketsscanned_sans_validation_croisee.md) |
| 8 | Store `events.js` : TTL de cache 5 min, contre 15 min pour les 3 stores de taxonomie voisins (convention documentée dans `CLAUDE.md`). Aucun pipeline temps réel ne met à jour les events (saisie manuelle uniquement, vérifié) — pas de justification opérationnelle trouvée pour un TTL plus court. Choix volontaire à documenter, ou aligner sur 15 min ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/147_events_store_ttl_5min_incoherent.md`](bugs/147_events_store_ttl_5min_incoherent.md) |
| 9 | `EventDrawerShell.vue` existe dans `components/events/drawers/` mais n'est utilisé par aucun des 3 drawers du domaine Événements (ils dupliquent son markup de header/footer) — ses seuls consommateurs réels sont côté Prévision, et son CSS contient un correctif de z-index/stacking spécifique à un besoin de ce domaine (overlay plein écran EventPredict). Migrer les 3 drawers dessus (retrofit du shell pour supporter leur footer variable + `persistent`), ou assumer la duplication comme deux besoins différents ? | Événements / Prévision | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/148_eventdrawershell_inutilise_duplication_markup.md`](bugs/148_eventdrawershell_inutilise_duplication_markup.md) |
| 10 | Restock — rôles « Technicien Logistic » / « PDV Superviseur » (permission `front.fb.restockBoard` sans `front.fb.restock`) : l'UI les laisse éditer mais `PUT/DELETE /restock-state` exige `front.fb.restock` seul → 403 permanent (le front ALERTE désormais, fix 2026-07-18, mais leur travail ne se sauvegarde toujours pas côté serveur). Élargir le PUT/DELETE aux deux permissions (l'usage réel suggère que restockBoard édite), ou assumer « Tableau de Réarmement = lecture seule » et gater le front en conséquence ? | Stock (Réarmement) / RBAC | Audit /analyse+predict+stock (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`bugs/19_restock_403_silencieux_front.md`](bugs/19_restock_403_silencieux_front.md), [`../../api-datafriday-staging/docs/bugs/31_restock_403_silencieux_roles_board.md`](../../api-datafriday-staging/docs/bugs/31_restock_403_silencieux_roles_board.md) |
| 11 | Inventaire — double persistance des comptages : chaque valeur est écrite dans `InventoryCount` (POST par item, pendant la saisie) ET `InventorySnapshot` (blob complet au « Save ») ; la lecture arbitre par fraîcheur. Aucune des deux tables n'est complète seule (le snapshot porte des champs que le DTO granulaire refuse). Quel chemin d'écriture est canonique — granulaire (et enrichir son DTO), snapshot (historique voulu ?), ou les deux assumés avec cette règle d'arbitrage documentée ? | Stock (Inventory) | Audit /analyse+predict+stock (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`bugs/183_double_persistance_comptages.md`](bugs/183_double_persistance_comptages.md) |
| 12 | Predict — versions dupliquées legacy : la vraie correction de la cascade de duplication (fiche 181) est une contrainte unique en DB, mais elle échouera tant que les doublons historiques existent (jusqu'à 76 observés pour un même event). Autorisation de nettoyer les doublons en prod (garder la plus récente par signature), avant de poser la contrainte ? | Prévision | Audit /analyse+predict+stock (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`bugs/181_cascade_duplication_versions_predict.md`](bugs/181_cascade_duplication_versions_predict.md) |
| 13 | Space Inventory — décomposition composant→sous-ingrédients retirée côté inventaire sur demande utilisateur (2026-07-18, « juste sur Space Inventory ») : un composant redevient une ligne comptable telle quelle. Le Restock, lui, conserve l'éclatement F6 (« on ne commande pas un composant ») → identités désormais **asymétriques** : l'inventaire compte « Pickles Auxerre », le restock réarme « Badiane »/« Canelle » — le netting comptage↔réarmement ne joint plus sur ces lignes. Aligner le restock (ne plus éclater non plus), ou assumer l'asymétrie (comptage au composant, achat aux ingrédients) ? | Stock (Inventory/Réarmement) | Demande utilisateur (session 2026-07-18) | 2026-07-18 | 🔴 | `src/utils/inventoryUtils.js` (buildConsolidatedInventory), `src/utils/stockPlanning.js` (expandMenuItemStock), `tests/unit/componentDecomposition.spec.js` |
| 14 | Analyse — les ventes de PdV **non mappés** (aucune ligne `WeezeventLocationShopMapping`) apparaissent désormais aussi dans les vues **par article** comme « Non rattachés » (bucket gris), alignées sur le comportement shop-level (fix back 103, 2026-07-18) : les totaux item-level **augmentent** par rapport à avant (les ventes n'étaient pas comptées, elles étaient supprimées). Comportement voulu, ou faut-il un toggle « inclure les non rattachés » ? | Analyse & agrégation | Audit bug items vides (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md) |
| 15 | Mapping PdV — `WeezeventLocationShopMapping.weezeventLocationId` porte deux conventions sans discriminant : id de **location** (wizard actuel, jointures `spaces.service.ts`/RPC granular) vs id de **merchant** (legacy, `space-aggregation.service.ts:184-186`, commentaire du modèle `schema.prisma:2293`). Les lignes keyées merchant ne matchent jamais la jointure item-level par `locationId` (ventes en « Non rattachés » au lieu d'être attribuées au PdV). Migrer les lignes legacy (re-keyer location), ou supporter les deux clés dans les jointures ? Le SQL Q2 de la fiche back 103 mesure la répartition réelle par tenant. | Analyse & agrégation / Intégrations | Audit bug items vides (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md), Piège n°3 de [`modules/02_ANALYSE.md`](modules/02_ANALYSE.md) |
| 16 | Analyse — attribution event↔ventes : l'item-level (`getEventTimelineBatch`) joint par **fenêtre de dates** `[eventDate, eventEndDate+1j)` de l'`Event` DataFriday, la RPC shop-details par **lien Weezevent stocké** puis égalité de DATE (fiche back 21). Deux events le même jour ou un décalage timezone/multi-jours attribuent différemment selon la vue. Quelle règle est canonique — fenêtre de dates ou lien stocké ? | Analyse & agrégation | Audit bug items vides (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../api-datafriday-staging/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md), [`../../api-datafriday-staging/docs/bugs/21_jointure_event_weezevent_par_date_seule.md`](../../api-datafriday-staging/docs/bugs/21_jointure_event_weezevent_par_date_seule.md) |
| 17 | Analyse — KPI « CA moyen par événement » : trois formules concurrentes coexistent avec deux définitions d'« event valide » affichées côte à côte sur la même carte (valeur = `useMetricsCalculator.js:39-80`, repli sur tous les events si aucun n'a de CA ; variation % = `AnalyseView.vue` `itemTotals`, règle stricte CA > 0 ; 3e variante store `totalsForEventIds`). Quelle définition est canonique (repli ou strict) pour unifier les trois ? | Analyse & agrégation | Audit modules (bug #9 de `modules/02_ANALYSE.md`), session 2026-07-18 | 2026-07-18 | 🔴 | `src/composables/useMetricsCalculator.js:39-80`, `src/store/modules/analyse.js:71-107`, `src/components/analyse/AnalyseView.vue` (itemTotals) |
| 18 | Event Predict Stock up — un article **combo** (`comboItem='Yes'`) avec `readyForSale='Yes'` est compté comme 1 pièce, jamais explosé en ses constituants (`expandMenuItem` ne consulte que `readyForSale`). Pour la préparation/réarmement, un combo doit-il être explosé en ses articles constitutifs, ou compté tel quel ? ⚠️ La même cascade est dupliquée à dessein côté backend (`logistics.service.ts` `deriveSales`) et côté inventaire (`buildConsolidatedInventory`) — la réponse s'applique aux trois. | Prévision / Stock | Audit modules (bug #2 de `modules/01_EVENT_PREDICT_ALGORITHME.md`), session 2026-07-18 | 2026-07-18 | 🔴 | [`bugs/188_stockup_explosion_ignore_comboitem.md`](bugs/188_stockup_explosion_ignore_comboitem.md) |
| 24 | RBAC / BUG-038 — la propagation d'une nouvelle permission **par défaut** vers les tenants **déjà créés** repose sur une procédure **manuelle** : ajouter le code dans `ADDITIVE_ROLE_GRANTS` (`prisma/backfill-rbac.ts`) puis lancer `npm run rbac:backfill`. Le mécanisme est **additif et sûr** (ne retire jamais rien, ne resynchronise pas aveuglément pour préserver les personnalisations admin), mais **fragile** : un dev peut ajouter une permission au seed `SYSTEM_ROLES` et oublier cette étape (cause du « piège » de BUG-038). Faut-il **automatiser** ce backfill (le lancer au déploiement / au boot), ou **conserver la procédure manuelle** désormais documentée (+ commentaire de rappel ajouté au-dessus de `SYSTEM_ROLES`) ? Contrainte : toute automatisation doit rester **curée/additive** — ne jamais ré-ajouter une permission qu'un admin a retirée à dessein. | Auth & onboarding (RBAC) | Résolution BUG-038 (Claude, session 2026-07-20) | 2026-07-20 | 🔴 | [`../../backend/docs/bugs/38_clonage_role_sans_resync_permissions.md`](../../backend/docs/bugs/38_clonage_role_sans_resync_permissions.md) |

## Questions résolues

| # | Question | Réponse (résumé) | Repliée dans |
|---|---|---|---|
| 1 | Sur `/suppliers`, le formulaire front rend `email`, `phone`, `address`, `city`, `postcode` obligatoires, alors que le modèle Prisma `Supplier` et `CreateSupplierDto` ne rendaient obligatoire que `name`. Faut-il aligner le front sur le backend ou durcir le backend ? | Décidé par l'utilisateur le 2026-07-16 : durcir le backend pour matcher le front. `contactName` inclus dans le même durcissement (également obligatoire côté front, oublié dans la question initiale). Schema Prisma + `CreateSupplierDto` mis à jour (email/tel/address/city/postcode/contactName non-optionnels) ; migration écrite avec backfill des lignes existantes (3 à 6 lignes selon le champ, sur 15 fournisseurs) et **appliquée le 2026-07-16 via `prisma migrate deploy`** (conformément à [ADR-0002 (backend)](../../backend/docs/adr/0002_migrations_manuelles_jamais_plateforme.md), sur autorisation explicite de l'utilisateur) contre la base pointée par `backend/.env` — vérifié en base (`\d "Supplier"` : colonnes `email/tel/address/city/postcode/contactName` en `NOT NULL`). | `backend/prisma/schema.prisma` (model Supplier), `backend/src/features/suppliers/dto/create-supplier.dto.ts`, `backend/prisma/migrations/20260716170000_supplier_required_contact_fields/` |
| 10 | `TaxonomyImportDrawer.vue` (import CSV de `/event-categories` et `/event-subcategories`) : rien n'obligeait à mapper la colonne "type"/"catégorie" parente ni à résoudre chaque valeur brute vers un id existant avant de cliquer "Importer". Bloquer (précédent `MarketPriceCsvImportDrawer.vue`) ou auto-créer par nom (précédent `MenuItemCsvImportDrawer.vue`, BUG-110/111/112) ? | Décidé par l'utilisateur le 2026-07-18 : **bloquer**, pas auto-créer. Raison : `EventType`/`EventCategory` est une taxonomie à faible cardinalité résolue **par nom** dans le moteur de scoring Event Predict (`predictiveAnalytics.js`) — contrairement au catalogue Menu Items (haute cardinalité, le CSV *est* la façon normale d'y créer des référentiels), un typo auto-créé ici fragmenterait silencieusement les events comparables utilisés pour la prédiction. `TaxonomyImportDrawer.vue` mis à jour : `canProceed` exige le mapping de la colonne FK parente, nouveau computed `valuesFullyMapped` bloque le bouton "Importer" tant qu'une valeur reste sur "Ignorer", alerte visible à l'étape de mapping. | [`bugs/151_taxonomyimportdrawer_fk_parente_non_forcee_avant_import.md`](bugs/151_taxonomyimportdrawer_fk_parente_non_forcee_avant_import.md) |
