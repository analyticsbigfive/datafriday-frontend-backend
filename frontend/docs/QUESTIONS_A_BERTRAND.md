# Questions à Bertrand — tracker de clarification

> On n'a pas de base de code auto-porteuse : la compréhension fonctionnelle réelle passe par
> Bertrand (review, test, validation). Ce fichier évite que chacun tranche seul dans son coin une
> incompréhension du code ou d'une fonctionnalité — ce qui est la source la plus fréquente de bugs
> et de conflits sur ce projet (voir [`docs/bugs/00_INDEX.md`](bugs/00_INDEX.md), plusieurs bugs
> viennent de deux devs ayant chacun implémenté sa propre interprétation de la même règle — ex.
> le gating Team, [BUG côté front](bugs/) vs comportement attendu).

## Comment l'utiliser

0. **Bertrand est Product Owner, pas architecte.** Ce fichier n'est PAS une liste générale
   d'incertitudes techniques — c'est le canal pour les **règles métier / décisions produit** que
   seul Bertrand peut trancher. Avant d'ajouter une ligne, se demander : *est-ce que le code, la
   base, ou une convention déjà établie ailleurs dans le repo répond déjà à cette question ?* Si
   oui → ce n'est pas une question, c'est une vérification à faire (voir `docs/bugs/`,
   `docs/modules/`, ou lire le code directement). Est-ce que c'est un choix d'architecture, de
   schéma, de performance, ou de refactor ? → c'est une décision **technique**, elle revient à
   Ulrich (lead), pas à Bertrand — la tracer dans
   [`DECISIONS_TECHNIQUES.md`](DECISIONS_TECHNIQUES.md) ou directement dans la fiche
   bug/module concernée, pas ici. Seule reste ici une question qui est *authentiquement* de la
   règle métier/produit, sans réponse possible en lisant le code ou la donnée.
1. Bloqué sur une incompréhension **produit** (règle métier, comportement attendu, arbitrage entre
   deux usages) qui ne se résout ni par le code ni par la donnée → ajouter une ligne ci-dessous,
   statut 🔴.
2. Poser la question à Bertrand.
3. Une fois répondu : mettre à jour la doc canonique concernée (page de module dans
   [`docs/modules/`](modules/00_INDEX.md), [`docs/adr/`](adr/00_INDEX.md), ou
   [`docs/bugs/`](bugs/00_INDEX.md) si la réponse révèle un bug) — **et le code si la réponse
   implique un renommage/commentaire/refactor** — puis lier la mise à jour ici et passer le
   statut à 🟢.
4. **Ne jamais merger un code qui repose sur une hypothèse encore marquée 🔴.**

Même mécanique pour une fonctionnalité neuve : confronter le cahier des charges au code existant,
lister ici ce qui reste ambigu **au niveau produit**, trancher avec Bertrand avant d'écrire le code
définitif.

> **Revue du 2026-07-24** : les 23 questions alors ouvertes dans ce tracker, plus 4 questions
> dormantes retrouvées dans l'ancien tracker backend (`backend/docs/old/QUESTIONS_A_BERTRAND.md`,
> jamais fusionnées jusqu'ici), ont été rejouées une à une contre le code/schéma/données réels — 27
> au total. Résultat : **6 étaient déjà résolues dans le code** (le tracker n'avait juste jamais été
> mis à jour — voir [« Fermées »](#fermées--déjà-résolues-dans-le-code) plus bas), **12 étaient en
> réalité des décisions techniques/architecture** (+2 sous-points séparés des questions #25 et #28,
> narrowées pour ne garder que leur volet produit) — pas des questions produit, déplacées vers
> [`DECISIONS_TECHNIQUES.md`](DECISIONS_TECHNIQUES.md) — il n'en restait que **9** authentiquement
> pour Bertrand.

## Questions ouvertes

> Uniquement des règles métier/produit sans réponse possible en lisant le code ou la donnée. Les
> décisions techniques/architecture qui vivaient ici avant le 2026-07-24 sont dans
> [`DECISIONS_TECHNIQUES.md`](DECISIONS_TECHNIQUES.md) ; les questions déjà résolues dans le code
> sont dans [« Fermées »](#fermées--déjà-résolues-dans-le-code) ci-dessous.

| # | Question | Domaine | Posée par | Date | Statut | Repliée dans |
|---|---|---|---|---|---|---|
| 2 | Import CSV `/events` : les colonnes `performerName`/`sponsor`/`openingActName`/`allSessions` sont toujours proposées au mapping (`CsvImportDrawer.vue`) mais aucun champ `Event` ne les stocke. **Mise à jour 2026-07-27** : BUG-136 a depuis corrigé le risque immédiat (ces champs sont désormais silencieusement retirés du payload avant `POST /events`, plus de 400) — mais ça déplace le problème plutôt que de le trancher : le mapping continue d'afficher des colonnes qui n'écrivent plus rien nulle part, sans avertir l'utilisateur. La vraie question produit reste entière : le métier veut-il *réellement* suivre performer/sponsor/opening-act sur les events (on ajoute les champs), ou ces colonnes n'ont jamais correspondu à un besoin réel (on les retire du mapping, point) ? | Événements | Audit domaine Événements (Claude, session 2026-07-17) | 2026-07-17 | 🔴 | [`bugs/136_csvimportdrawer_champs_hors_dto_400_garanti.md`](bugs/136_csvimportdrawer_champs_hors_dto_400_garanti.md) |

| 13 | Space Inventory — décomposition composant→sous-ingrédients retirée côté inventaire sur demande utilisateur (2026-07-18, « juste sur Space Inventory ») : un composant redevient une ligne comptable telle quelle. Le Restock, lui, conserve l'éclatement F6 (« on ne commande pas un composant ») → identités désormais **asymétriques** : l'inventaire compte « Pickles Auxerre », le restock réarme « Badiane »/« Canelle » — le netting comptage↔réarmement ne joint plus sur ces lignes. Aligner le restock (ne plus éclater non plus), ou assumer l'asymétrie (comptage au composant, achat aux ingrédients) ? Vérifié : les deux comportements sont des décisions récentes et explicites en sens opposé (`inventoryUtils.js` vs `stockPlanning.js`) — aucune convention de code ne permet de trancher entre les deux, seul un choix produit le peut. | Stock (Inventory/Réarmement) | Demande utilisateur (session 2026-07-18) | 2026-07-18 | 🔴 | `src/utils/inventoryUtils.js` (buildConsolidatedInventory), `src/utils/stockPlanning.js` (expandMenuItemStock), `tests/unit/componentDecomposition.spec.js` |

| 14 | Analyse — les ventes de PdV **non mappés** (aucune ligne `WeezeventLocationShopMapping`) apparaissent désormais aussi dans les vues **par article** comme « Non rattachés » (bucket gris), alignées sur le comportement shop-level (fix back 103, 2026-07-18) : les totaux item-level **augmentent** par rapport à avant. Comportement voulu, ou faut-il un toggle « inclure les non rattachés » ? Mesuré (audit DB 2026-07-24) : ce bucket pèse **29,8 % des transactions / 30,1 % du CA** — l'échelle rend la question consequente, mais ne dit pas *comment l'afficher par défaut* (toujours visible, caché avec opt-in, visible avec opt-out) : pur choix produit d'affichage. | Analyse & agrégation | Audit bug items vides (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md`](../../backend/docs/bugs/103_event_timeline_articles_vides_jointure_mapping.md) |

| 20 | Post-event Inventory — « Miss € » (valorisation du manquant) calculé au **coût unitaire** (`menuItemCostMap`, cohérent avec une perte de stock). Le vouloir plutôt au **prix de vente** (manque à gagner) ? Vérifié : aucun autre écran de l'app ne valorise déjà un écart de stock en € (`StockReconciliation.lines` — le seul autre endroit où des écarts sont archivés — n'a même pas de champ monétaire) : aucun précédent de code à suivre, choix produit pur. | Stock (Post-event Inventory) | Feature réconciliation (Claude, session 2026-07-20) | 2026-07-20 | 🔴 | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §7.5 |

| 25 | Pre-event Inventory — l'« attendu » serveur somme **toutes** les raisons de `StockMovement` (DELIVERY, TRANSFER, EXPIRY, SALE, INVENTORY_RESET, OTHER), alors que la spec métier le formule « livraisons − retraits ». **Recentré le 2026-07-24** : le volet fenêtre/ancrage (bornes, quel event de référence) est traité par la réponse à #24 (reset auto sur Door opening) et devient un suivi technique (voir `DECISIONS_TECHNIQUES.md`) — reste uniquement la question métier : quelles raisons de mouvement comptent comme un « retrait » légitime dans l'attendu ? `EXPIRY` semble clairement oui ; `INVENTORY_RESET` (un SET, pas un delta) et `SALE` (déjà capturées par le comptage post-event, cf. #31) sont plus ambigus — définition à trancher avec Bertrand. | Stock (Pre-event / Logistic) | Vérification logique Pre/Post-event vs spec (JLH, session 2026-07-20) | 2026-07-20 | 🔴 | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §9.3 (D1), `backend/src/features/inventory/inventory.service.ts:342-415` |

| 28 | RH — l'algorithme de staffing de `Règles RH.xlsx` est signalé « bugué » par le métier (2026-07-21) : quelles formules sont fausses ? Caissiers = ROUNDUP((CA − Goal/3)/Goal) plafonné max TPE ; runners = ROUNDDOWN((CA + Goal/2)/Goal) − Resp. PDV ; RZ = front/15 ; traitement fontaines/tireuses (cellule « A voir » du xlsx) ; seuils alertes 1000/2500 €/pers ; catégorie RH par PDV (l'enum `ShopType` actuel ne correspond pas aux catégories du xlsx). **Recentré le 2026-07-24** : la source du « mètre linéaire » (`SpaceElement.width` ?) est une simple vérification de mapping de données, déplacée vers `DECISIONS_TECHNIQUES.md` — reste uniquement la validation des formules/seuils/catégories elles-mêmes, qui n'a pas d'autre source que le métier. | RH / Prévision | Analyse `Règles RH.xlsx` (JLH / Claude, session 2026-07-21) | 2026-07-21 | 🔴 | [`modules/11_RH_STAFFING.md`](modules/11_RH_STAFFING.md) §6.2 |

| 30 | RH — deux notions de staffing coexistent désormais : `ElementStaff` (staffing statique par élément×config du builder, servi par builder-v2 `staffByConfig`) et la bibliothèque RH `/hr` (positions/taux par supplier). À terme : fusionner, faire de la bibliothèque la source des taux du builder, ou garder séparés ? Vérifié : les deux servent aujourd'hui des workflows différents (config statique du builder vs taux gérés par fournisseur RH) — ils ne convergent que si le métier veut que les taux RH pilotent désormais le coût du builder ; c'est une question fonctionnelle sur la relation entre les deux écrans, pas un choix de modèle de données que le code peut trancher seul. | RH / Espaces & builder | Branchement Edit HR (JLH / Claude, session 2026-07-21) | 2026-07-21 | 🔴 | [`modules/11_RH_STAFFING.md`](modules/11_RH_STAFFING.md) §3 |

| 31 | Pre-event Inventory — la vue « Stock » de la Logistique déduit une **consommation dérivée des ventes** (`deriveSalesRaw` → `explodeSalesToConsumption`) en plus des `StockMovement` ; l'« attendu » pre-event (`computeExpected`) ne rejoue QUE les `StockMovement`. Si des ventes surviennent hors match dans la fenêtre post→pre (event intercalé sans comptage, vente hors match), l'attendu les ignore et surestime le stock. Faut-il déduire aussi la consommation dérivée des ventes, ou assumer « mouvements seuls » ? Vérifié : la réponse à #24 (reset auto sur Door opening) ne supprime pas ce cas — non implémentée à ce jour, et le doc `10_POST_EVENT_INVENTORY.md` §8.5 liste toujours ce point comme séparément ouvert. Le reste du système (le `reset()` Logistic lui-même) fait déjà confiance à cette même consommation dérivée par ailleurs — question de confiance/exactitude métier, pas de faisabilité technique. | Stock (Pre-event / Logistic) | Fix BUG-232 (JLH / Claude, session 2026-07-23) | 2026-07-23 | 🔴 | [`bugs/232_pre_event_expected_non_normalise_negatifs.md`](bugs/232_pre_event_expected_non_normalise_negatifs.md), `backend/src/features/logistics/logistics.service.ts` (`deriveSalesRaw`) |

| 33 | Inventaire — lignes `InventoryCount` avec `shopId=NULL` : quelle sémantique métier (comptage « niveau espace » ?). Elles sont inadressables par la shape front `{[shopId]: …}` et donc ignorées en lecture — BUG-94 (backend) contourne le symptôme en repliant sur le snapshot, mais la fiche du bug le dit elle-même explicitement : la sémantique « reste à trancher ». Si ces lignes doivent s'afficher : convenir d'une clé sentinelle + adaptation front. *(Question migrée depuis `backend/docs/old/QUESTIONS_A_BERTRAND.md` #1, jamais reportée dans ce tracker jusqu'au 2026-07-24.)* | Stock (Inventory) | Audit /analyse+predict+stock (Claude, session 2026-07-18) | 2026-07-18 | 🔴 | [`backend/docs/bugs/94_buildinventorycounts_perd_lignes_shopid_null.md`](../../backend/docs/bugs/94_buildinventorycounts_perd_lignes_shopid_null.md) |

## Fermées — déjà résolues dans le code

> Trouvées lors de la revue du 2026-07-24 : ces questions avaient déjà leur réponse implémentée en
> code (souvent avec une fiche bug 🟢 Corrigé à l'appui) au moment où elles ont été posées ou peu
> après — le tracker n'avait simplement jamais été mis à jour. Aucune n'a besoin de Bertrand.
>
> **Ajout 2026-07-27** : re-vérification des 10 questions alors ouvertes contre le code réel —
> #34 avait elle aussi déjà sa réponse implémentée (voir ligne dédiée ci-dessous). Les 8 autres
> (#2 reformulée, #13, #14, #20, #25, #28, #30, #31, #33) restent authentiquement ouvertes,
> inchangées depuis leur dernière mise à jour.

| # | Question (résumé) | Constat vérifié dans le code (2026-07-24) | Repliée dans |
|---|---|---|---|
| 34 | Live — le bouton ◉ sur la carte Home (`SpaceItem.vue`, greffe A) doit-il signaler un event live **sans** que l'utilisateur entre dans l'espace ? Options envisagées : (a) polling `live-status` par carte, ou (b) ◉ visible seulement après être entré dans l'espace. | **Vérifié 2026-07-27** : déjà tranché et livré le 2026-07-23 (commit `3d7e53a`, "feat(live): bouton ◉ réel + onglet Inventaire live (v2)") — option (a) retenue, en version allégée : `SpaceItem.vue` appelle `getSpaceLiveStatus()` (`GET /spaces/:id/live-status`) au montage de chaque carte visible sur la Home (pas un `setInterval` périodique, mais un fetch par montage, gated par la permission `front.fb.live`). `11_LIVE.md` §10.6/§12 et ce tracker n'avaient simplement jamais été mis à jour pour le refléter. | [`modules/11_LIVE.md`](modules/11_LIVE.md) §10.6, §8bis (greffe A), `SpaceItem.vue`, `space.api.js` (`getSpaceLiveStatus`) |
| 3 | Cascade delete silencieuse `EventType`/`EventCategory`, incohérence supposée avec `ProductType`/`ComponentType` sans garde | Backend bugs 75 (Events), 79 (ProductType), 81 (ComponentType) sont tous 🟢 Corrigé — vérifié en code : `events.service.ts:388-399,478-488` et `menu-items.service.ts:1633-1648` ont désormais la même garde `ConflictException`. Plus d'incohérence : toutes les taxonomies bloquent la suppression avec dépendants. | [`backend/docs/bugs/75_eventtype_eventcategory_delete_cascade_sans_garde.md`](../../backend/docs/bugs/75_eventtype_eventcategory_delete_cascade_sans_garde.md) |
| 4 | `Team` sans contrainte `@@unique` | `schema.prisma:2186` porte `@@unique([tenantId, eventCategoryId, eventSubcategoryId, name])`, migration `20260718120000_team_unique_constraint` déployée et vérifiée en base (audit 2026-07-24 : contrainte présente, 0 doublon/11 teams). Backend bug 70 corrigé, statut de l'index backend mis à jour du même coup (il disait encore « non déployé » par erreur). | [`backend/docs/bugs/70_team_duplicate_toctou_sans_unique_index.md`](../../backend/docs/bugs/70_team_duplicate_toctou_sans_unique_index.md) |
| 5 | `EventPredictVersion.create()` sans vérification `eventId` | `predict-versions.service.ts:26-31` fait déjà `findFirst({ id: eventId, tenantId })` + `NotFoundException` avant la création. Backend bug 76 🟢 Corrigé. | [`backend/docs/bugs/76_predictversion_create_eventid_non_verifie.md`](../../backend/docs/bugs/76_predictversion_create_eventid_non_verifie.md) |
| 6 | Deux implémentations de création de catégorie (drawer inline vs `EventCategoryDialog.vue`) | `EventCategoryDialog.vue` porte désormais `category`/`allowCreateType`/`isEdit` ; `EventsCategorieListView.vue:95-99` l'utilise avec `allow-create-type` — le drawer inline dupliqué a disparu. Décision (unifier sur le dialog partagé) prise et implémentée le 2026-07-18. | [`bugs/145_eventcategorielist_duplication_creation_categorie.md`](bugs/145_eventcategorielist_duplication_creation_categorie.md) |
| 7 | Pas de validation croisée `ticketsScanned ≤ ticketsSold` | `EventFormDrawer.vue:369,560` implémentent déjà un bandeau d'avertissement non bloquant (`ticketsScannedExceedsSold`) ; `submit()` volontairement non modifié (pas de blocage). Cohérent avec l'audit DB (0 violation réelle sur 38 events) — le bon choix était déjà livré. | [`bugs/146_eventformdrawer_ticketsscanned_sans_validation_croisee.md`](bugs/146_eventformdrawer_ticketsscanned_sans_validation_croisee.md) |
| 9 | `EventDrawerShell.vue` inutilisé par les 3 drawers Événements | Les 3 drawers (`EventFormDrawer.vue`, `CsvImportDrawer.vue`, `TaxonomyImportDrawer.vue`) importent et utilisent déjà `<EventDrawerShell>` (props `persistent`/`isDark` ajoutées). Migration faite le 2026-07-18. | [`bugs/148_eventdrawershell_inutilise_duplication_markup.md`](bugs/148_eventdrawershell_inutilise_duplication_markup.md) |

## Questions résolues

> Les questions #8, #17, #18, #22, #23, #24, #26, #27 ont été répondues par Bertrand le 2026-07-24
> (échange Bertrand Jame / Jean-Luc Houedanou / Emmanuel Kouevi), voir le détail et les réponses
> complémentaires issues de l'audit base de données dans
> [`REPONSES_QUESTIONS_2026-07-24.md`](REPONSES_QUESTIONS_2026-07-24.md). ⚠️ Une réponse validée
> ici ne veut pas dire un fix livré : chaque ligne marquée **⚠️** ci-dessous a sa doc canonique
> mise à jour mais **le code reste à modifier**.

| # | Question | Réponse (résumé) | Repliée dans |
|---|---|---|---|
| 22 | Live — **source du stock « en direct »** pour l'onglet Inventaire live : d'où vient le niveau de stock temps réel — décrément à partir des ventes entrantes, mouvements de Réarmement (Restock), ou une combinaison ? Et quel périmètre pour l'arbre (Shop → items / Item → shops) ? | Décidé par l'utilisateur le 2026-07-23, en deux temps : **(1) source = combinaison** — réutilisation telle quelle du calcul déjà en production du module Logistic (`StockLevel` pour les mouvements Restock + `deriveSalesRaw`/`explodeSalesToConsumption` pour le décrément par vente en temps réel, `backend/src/features/logistics/logistics.service.ts:847-938`) ; **(2) granularité = comportement par défaut**, comme le Réarmement — piloté par `readyForSale` sans exception, **pas** l'override de Space Inventory (question #13) qui court-circuite l'éclatement pour certains composants. Live suit donc la même logique que Restock, pas celle de Space Inventory. Périmètre : Shop → items ET Item → shops (index inversé), scopé aux éléments de type shop (pas les storages). | [`modules/11_LIVE.md`](modules/11_LIVE.md) §10.4, §8bis (greffe E), `api-datafriday-staging/docs/api/LIVE_API_GUIDE.md` §3 |
| 23 | Live — **cardinalité event/espace** : un espace peut-il avoir **plusieurs events live simultanément** à un instant T, ou un seul ? Détermine si le bouton ◉ et l'écran Live ciblent un event unique ou proposent une sélection. | Décidé par l'utilisateur le 2026-07-23 : **un seul event live par espace** à un instant T. Le bouton ◉ et l'écran Live ciblent donc toujours un event unique — pas de sélecteur multi-event à construire. | [`modules/11_LIVE.md`](modules/11_LIVE.md) §10.5, §12 |
| 24 | RBAC / BUG-038 — la propagation d'une nouvelle permission **par défaut** vers les tenants **déjà créés** reposait sur une procédure **manuelle** : ajouter le code dans `ADDITIVE_ROLE_GRANTS` (`prisma/backfill-rbac.ts`) puis lancer `npm run rbac:backfill`. Fragile : un dev pouvait ajouter une permission au seed `SYSTEM_ROLES` et oublier cette étape. Fallait-il automatiser (curé/additif) ou garder le manuel ? | Décidé par l'utilisateur le 2026-07-22 : **automatiser sans curation** — propager systématiquement toute permission nouvellement créée aux tenants existants ; à l'admin de la retirer ensuite s'il ne la veut pas. Implémenté : `ensureSystemPermissionCatalog` (`permission-catalog.ts`) détecte les codes tout juste créés en base (signal fiable — un code neuf n'a jamais pu être retiré par un admin) et les accorde automatiquement aux rôles métier existants de `SYSTEM_ROLES` qui les incluent par défaut, sur tous les tenants. `ADDITIVE_ROLE_GRANTS` supprimé (plus de liste à maintenir) ; `npm run rbac:backfill` inchangé côté usage. | [`../../backend/docs/bugs/38_clonage_role_sans_resync_permissions.md`](../../backend/docs/bugs/38_clonage_role_sans_resync_permissions.md) |
| 1 | Sur `/suppliers`, le formulaire front rend `email`, `phone`, `address`, `city`, `postcode` obligatoires, alors que le modèle Prisma `Supplier` et `CreateSupplierDto` ne rendaient obligatoire que `name`. Faut-il aligner le front sur le backend ou durcir le backend ? | Décidé par l'utilisateur le 2026-07-16 : durcir le backend pour matcher le front. `contactName` inclus dans le même durcissement (également obligatoire côté front, oublié dans la question initiale). Schema Prisma + `CreateSupplierDto` mis à jour (email/tel/address/city/postcode/contactName non-optionnels) ; migration écrite avec backfill des lignes existantes (3 à 6 lignes selon le champ, sur 15 fournisseurs) et **appliquée le 2026-07-16 via `prisma migrate deploy`** (conformément à [ADR-0002 (backend)](../../backend/docs/adr/0002_migrations_manuelles_jamais_plateforme.md), sur autorisation explicite de l'utilisateur) contre la base pointée par `backend/.env` — vérifié en base (`\d "Supplier"` : colonnes `email/tel/address/city/postcode/contactName` en `NOT NULL`). | `backend/prisma/schema.prisma` (model Supplier), `backend/src/features/suppliers/dto/create-supplier.dto.ts`, `backend/prisma/migrations/20260716170000_supplier_required_contact_fields/` |
| 10 | `TaxonomyImportDrawer.vue` (import CSV de `/event-categories` et `/event-subcategories`) : rien n'obligeait à mapper la colonne "type"/"catégorie" parente ni à résoudre chaque valeur brute vers un id existant avant de cliquer "Importer". Bloquer (précédent `MarketPriceCsvImportDrawer.vue`) ou auto-créer par nom (précédent `MenuItemCsvImportDrawer.vue`, BUG-110/111/112) ? | Décidé par l'utilisateur le 2026-07-18 : **bloquer**, pas auto-créer. Raison : `EventType`/`EventCategory` est une taxonomie à faible cardinalité résolue **par nom** dans le moteur de scoring Event Predict (`predictiveAnalytics.js`) — contrairement au catalogue Menu Items (haute cardinalité, le CSV *est* la façon normale d'y créer des référentiels), un typo auto-créé ici fragmenterait silencieusement les events comparables utilisés pour la prédiction. `TaxonomyImportDrawer.vue` mis à jour : `canProceed` exige le mapping de la colonne FK parente, nouveau computed `valuesFullyMapped` bloque le bouton "Importer" tant qu'une valeur reste sur "Ignorer", alerte visible à l'étape de mapping. | [`bugs/151_taxonomyimportdrawer_fk_parente_non_forcee_avant_import.md`](bugs/151_taxonomyimportdrawer_fk_parente_non_forcee_avant_import.md) |
| 19 | Post-event Inventory — « inventaire pré-événement » de la réconciliation : quelle définition exacte ? (posée le 2026-07-20, défaut initial = heuristique de date) | Résolue par l'arrivée de l'écran **Pre-event Inventory** (même session, JLH) : le pré-event du post-event(N) = **le comptage Pre-event Inventory du même événement** (`InventorySnapshot.kind='pre-event'`, cycle fermé) ; l'heuristique de date ne subsiste qu'en repli legacy pour les snapshots sans `kind`. | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §7.2/§8.1, `inventory.service.ts getPreEventInventory` |
| 19 | Live — **transport temps réel** : polling vs SSE vs WebSocket ? | Décidé par Ulrich (owner backend) le 2026-07-20, après recherche approfondie du code réel : **polling pour le v1**. SSE/WebSocket écartés — aucune nécessité identifiée, et aucune info du repo ne garantit le comportement du plan Render FREE sur des connexions longues. Découverte en cours de route : `event-timeline` lit `WeezeventTransaction` en direct (sans cache, sans dépendre d'une agrégation périodique) — sa fraîcheur est donc déjà quasi temps réel via le webhook + le cron de fallback ; un simple polling (15 s) suffit. En revanche `shop-details` dépend de `SpaceRevenueMinuteAgg`, dont l'agrégation **n'est jamais déclenchée automatiquement** aujourd'hui (seulement à la main via le wizard d'intégration) — **prérequis backend bloquant révélé** : câbler ce déclenchement automatique, et corriger BUG-19 (queue d'agrégation sans retry, `attempts: 1` en dur) avant d'en dépendre pour du live. | [`modules/11_LIVE.md`](modules/11_LIVE.md) §5 |
| 20 | Live — **définition de « event live »** : quel signal backend précis (dernier webhook < N min ? statut d'`Event` ?) et quel seuil N ? | Décidé par Ulrich le 2026-07-20 : signal = **au moins une vente réelle ingérée dans les 30 dernières minutes**, pour les shops mappés à l'event, dans la fenêtre `[eventStartDate, eventEndDate]` (même logique de jointure que `event-timeline`) — pas le timestamp brut du webhook. Raison : `Event.status` est un champ texte libre jamais écrit par le pipeline d'agrégation (inutilisable) ; `IntegrationWebhookEvent.createdAt` est alimenté mais BUG-26 (dédup webhook inopérante, `externalDeliveryId` jamais renseigné côté Weezevent) signifie qu'un simple retry Weezevent pourrait simuler un faux signal live sans vente réelle derrière. Seuil de 30 min = défaut raisonnable (creux normal entre ventes pendant un event), ajustable selon l'usage réel. | [`modules/11_LIVE.md`](modules/11_LIVE.md) §7 |
| 21 | Live — **forme de la route** : route dédiée `/spaces/:id/live`, ou paramètre `?live=1` sur l'écran Analyse ? | Décidé par Ulrich le 2026-07-20 : **route dédiée** `space-live`, `meta: { keepAlive: true, permission: 'front.fb.live' }` — pas un paramètre. Raison : Live inclut un **nouvel onglet Inventaire live**, qui n'est pas un simple mode d'affichage de l'Analyse existante (contrairement à Predict/Event Predict, qui eux justifient le pattern `?toolbox=` déjà en place) ; précédent le plus proche = `space-inventory`/`space-restock` (route dédiée + keepAlive). Découverte notable en cours de route : la permission `front.fb.live` **existe déjà** dans le catalogue RBAC depuis avant cette conception (assignée à « Analyste F&B » et « Achat F&B ») — aucun seed/backfill à faire, contrairement à ce que supposait la conception initiale. | [`modules/11_LIVE.md`](modules/11_LIVE.md) §9, §10.3 |
| 8 | Store `events.js` : TTL de cache 5 min, contre 15 min pour les 3 stores de taxonomie voisins — choix volontaire à documenter, ou aligner sur 15 min ? | Résolu le 2026-07-18 : aligné sur **15 min**, confirmé définitif le 2026-07-24 — clarification faite avec Bertrand : le « 2 min max pour le live » de sa réponse concerne le **besoin de fraîcheur propre au futur module Live** (mécanisme séparé, à construire avec ce module), **pas** une modification du TTL de `events.js`, partagé par le reste de l'app. Deux préoccupations distinctes, pas de confusion à faire. | [`bugs/147_events_store_ttl_5min_incoherent.md`](bugs/147_events_store_ttl_5min_incoherent.md) |
| 17 | Analyse — KPI « CA moyen par événement » : trois formules concurrentes, quelle définition est canonique pour unifier ? | Répondu par Bertrand le 2026-07-24 : **total des CA des events enregistrés ÷ nombre d'events enregistrés**, sans filtre `CA > 0` ni repli conditionnel. ⚠️ **Code non encore unifié** entre `useMetricsCalculator.js`, `analyse.js` (`totalsForEventIds`) et `AnalyseView.vue` (`itemTotals`). | [`modules/02_ANALYSE.md`](modules/02_ANALYSE.md) bug #9 |
| 18 | Event Predict Stock up — un combo `readyForSale='Yes'` doit-il être explosé en ses constituants, ou compté comme 1 pièce ? | Répondu par Bertrand le 2026-07-24 : **exploser en menu items constitutifs**, chaque constituant suivant ensuite les règles standard des menu items. S'applique aux 3 implémentations dupliquées à dessein (front `expandMenuItem`, backend `logistics.service.ts` `deriveSales`, inventaire `buildConsolidatedInventory`). ⚠️ **Code non encore modifié** aux 3 endroits. | [`bugs/188_stockup_explosion_ignore_comboitem.md`](bugs/188_stockup_explosion_ignore_comboitem.md) |
| 22 | Capture Figma : quel contenu attendu pour l'onglet Staff (Event Predict Configuration Settings) ? | Répondu par Bertrand le 2026-07-24 : **voir la Google Slide interface** (spec de référence pour le contenu). Reste planifié séparément de `feat/postEventInventory` — implémentation toujours bloquée sur la validation de l'algo RH ([question #28](#questions-ouvertes), toujours ouverte). | [`modules/11_RH_STAFFING.md`](modules/11_RH_STAFFING.md) §6.1, §7 |
| 23 | Pre-event Inventory — élargissement `spaceInventory`/`preInventoryExpected` aux rôles « Directeur de site »/« Chef exécutif » : à valider ? | Répondu par Bertrand le 2026-07-24 : **élargissement validé**. ⚠️ Seed RBAC idempotent **reste à rejouer en prod** pour que l'élargissement s'applique aux tenants existants. | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §8.3 |
| 24 | Pre-event Inventory — un Inventory Reset logistique intercalé n'est pas déduit de l'attendu (double comptage possible) : borner la fenêtre, ou intégrer le reset ? | Répondu par Bertrand le 2026-07-24 : **l'inventaire reste attaché à son event, avec un reset automatique déclenché à l'ouverture des portes (« Door opening »)**, réconciliation sauvegardée rattachée à l'événement. L'ancrage devient l'event lui-même plutôt qu'une fenêtre de mouvements ouverte — recoupe la question 25 (bornes de fenêtre, toujours ouverte) et 31. ⚠️ **Non implémenté** : reste à définir le déclencheur technique du reset. | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §8.5 |
| 26 | Post-event Inventory — métriques de réconciliation stockées verbatim depuis le client, sans contrôle serveur : acceptable, ou recalcul serveur requis ? | Répondu par Bertrand le 2026-07-24 : **« bonne suggestion, mettre en place »** le recalcul/contrôle serveur ; résultat visible **uniquement par Directeur de site et Admin**. ⚠️ **Non implémenté**. | [`modules/10_POST_EVENT_INVENTORY.md`](modules/10_POST_EVENT_INVENTORY.md) §9.3 (D3) |
| 27 | Photos base64 dans `MenuItem.picture` (une ligne à 915 ko) : migrer vers Storage, normaliser les `''`, redimensionner à l'upload ? | Répondu par Bertrand le 2026-07-24 : **redimensionnement client nécessaire** pour limiter la taille en mémoire (recoupe BUG-082). Migration des 3 lignes legacy vers Supabase Storage et normalisation des 79 `''` en `NULL` à traiter dans la foulée. ⚠️ **Non implémenté**. | [`bugs/227_shop_items_photo_base64_dupliquee_par_pdv.md`](bugs/227_shop_items_photo_base64_dupliquee_par_pdv.md), [`bugs/82_menu_items_upload_image_sans_validation.md`](bugs/82_menu_items_upload_image_sans_validation.md) |
